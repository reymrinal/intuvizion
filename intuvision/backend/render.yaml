services:
  - type: web
    name: intuvizion-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT --workers 1 --timeout 300
    envVars:
      - key: GEMINI_API_KEY
        sync: false
